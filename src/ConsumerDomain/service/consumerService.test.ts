import { ConsumerService } from './consumerService';
describe('Consumer service', () => {
  it('TODO', () => {
    const service = new ConsumerService();
    expect(service.performAction()).toBeTruthy();
  });
});
